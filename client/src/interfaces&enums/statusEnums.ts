export enum StatusEnum {
  IDLE = 'idle',
  LOADING = 'loading',
  FAILED = 'failed',
}

export enum DeviceTypes {
  PHONE = 'smartphone',
  TABLET = 'tablet',
  DESKTOP = 'desktop',
}

export enum PromptSizes {
  QUARTER = '25%',
  HALF = '50%',
  TWO_QUARTERS = '75%',
  FULL = '100%',
}
